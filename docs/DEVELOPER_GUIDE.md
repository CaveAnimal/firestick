# Developer Guide

- Build: `mvn clean package`
- Run: `mvn spring-boot:run`
- UI dev: see `ui/README.md` (Vite dev server with proxy to 8080).
- Tests: `mvn test`; view coverage under `target/site/jacoco`.
